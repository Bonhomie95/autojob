# ── Core engine (job scraping, scoring, document generation, mailing) ──
flask>=3.0.0
groq>=0.9.0
httpx[socks]>=0.27.0
python-docx>=1.1.0
requests[socks]>=2.31.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
lxml>=4.9.0
fake-useragent>=1.4.0
feedparser>=6.0.0
python-dateutil>=2.8.0
werkzeug>=3.0.0
reportlab>=4.0.0
docx2pdf>=0.1.8
pdfplumber>=0.11.0
pymupdf>=1.24.0            # robust PDF text extraction + page rasterisation
pypdf>=4.2.0              # extra PDF text-extraction fallback
pytesseract>=0.3.10       # OCR for scanned/image-only CVs (needs tesseract-ocr)
apscheduler>=3.10.0
playwright>=1.44.0
pytz>=2024.1

# ── SaaS platform layer (Phase 1+) ──
# Data / ORM / migrations
Flask-SQLAlchemy>=3.1.0
Flask-Migrate>=4.0.0
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0          # Postgres driver (prod)

# Auth / security
Flask-Login>=0.6.3
Flask-WTF>=1.2.0
WTForms>=3.1.0
email-validator>=2.1.0
Flask-Limiter>=3.5.0
cryptography>=42.0.0            # Fernet encryption for per-user credentials

# Validation
pydantic>=2.6.0

# Background jobs (Phase 5)
celery>=5.3.0
redis>=5.0.0

# Object storage (Phase 4, optional S3 backend)
boto3>=1.34.0

# Observability (Phase 6, optional)
sentry-sdk[flask]>=1.40.0

# Production server (Phase 7)
gunicorn>=21.2.0

# ── Test tooling ──
pytest>=8.0.0
pytest-flask>=1.3.0
